---
# Source: alloy/templates/configmap-env.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: alloy-env
  namespace: monitoring
data:
  CLUSTER_NAME: "kubernetes" # <--- UBAH INI sesuai nama cluster Anda
  MONITORING_ENDPOINT: "192.171.171.26:4317" # <--- UBAH INI dengan IP Server Anda
---
# Source: alloy/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
automountServiceAccountToken: true
metadata:
  name: alloy
  namespace: monitoring
  labels:
    helm.sh/chart: alloy-1.8.1
    app.kubernetes.io/name: alloy
    app.kubernetes.io/instance: alloy
    app.kubernetes.io/version: "v1.16.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: alloy
    app.kubernetes.io/component: rbac
---
# Source: alloy/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: alloy
  namespace: monitoring
  labels:
    helm.sh/chart: alloy-1.8.1
    app.kubernetes.io/name: alloy
    app.kubernetes.io/instance: alloy
    app.kubernetes.io/version: "v1.16.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: alloy
    app.kubernetes.io/component: config
data:
  config.alloy: |-
    logging {
      level  = "info"
      format = "logfmt"
    }

    // --- METRICS PIPELINE ---
    
    // 1. Discovery: Cari service kube-state-metrics
    discovery.kubernetes "services" {
      role = "service"
    }

    // 1b. Discovery: Cari seluruh endpoints dari service (untuk DaemonSet)
    discovery.kubernetes "endpoints" {
      role = "endpoints"
    }

    discovery.relabel "kube_state_metrics" {
      targets = discovery.kubernetes.services.targets
      rule {
        source_labels = ["__meta_kubernetes_service_name"]
        regex         = "kube-state-metrics"
        action        = "keep"
      }
    }

    // 2. Scrape: Ambil data metrics dari kube-state-metrics
    prometheus.scrape "kube_state_metrics" {
      targets    = discovery.relabel.kube_state_metrics.output
      forward_to = [prometheus.relabel.add_cluster.receiver]
      job_name   = "kube-state-metrics"
    }

    // --- NODE EXPORTER PIPELINE ---

    // 3. Discovery: Cari service node-exporter
    discovery.relabel "node_exporter" {
      targets = discovery.kubernetes.endpoints.targets
      rule {
        source_labels = ["__meta_kubernetes_service_name"]
        regex         = "node-exporter"
        action        = "keep"
      }
      // Mapping nama node ke label 'node'
      rule {
        source_labels = ["__meta_kubernetes_endpoint_node_name"]
        target_label  = "node"
      }
    }

    // 4. Scrape: Ambil data metrics dari node-exporter (CPU, RAM, Disk, Network)
    prometheus.scrape "node_exporter" {
      targets    = discovery.relabel.node_exporter.output
      forward_to = [prometheus.relabel.add_cluster.receiver]
      job_name   = "node-exporter"
    }

    // --- KUBELET & CADVISOR PIPELINE ---
    // Diperlukan untuk metric spesifik dari setiap pod/container (seperti CPU/RAM per Pod)

    discovery.kubernetes "nodes" {
      role = "node"
    }

    discovery.relabel "kubelet" {
      targets = discovery.kubernetes.nodes.targets
      rule {
        source_labels = ["__meta_kubernetes_node_name"]
        target_label  = "node"
      }
    }

    prometheus.scrape "kubelet" {
      targets    = discovery.relabel.kubelet.output
      forward_to = [prometheus.relabel.add_cluster.receiver]
      job_name   = "kubelet"
      scheme     = "https"
      bearer_token_file = "/var/run/secrets/kubernetes.io/serviceaccount/token"
      tls_config {
        insecure_skip_verify = true
      }
    }

    prometheus.scrape "kubelet_cadvisor" {
      targets    = discovery.relabel.kubelet.output
      forward_to = [prometheus.relabel.add_cluster.receiver]
      job_name   = "kubelet"
      scheme     = "https"
      metrics_path = "/metrics/cadvisor"
      bearer_token_file = "/var/run/secrets/kubernetes.io/serviceaccount/token"
      tls_config {
        insecure_skip_verify = true
      }
    }

    // --- GLOBAL RELABELING ---
    // Inject cluster name to all metrics
    prometheus.relabel "add_cluster" {
      forward_to = [otelcol.receiver.prometheus.default.receiver]
      rule {
        target_label = "cluster"
        replacement  = coalesce(sys.env("CLUSTER_NAME"), "kubernetes")
      }
    }

    // --- BRIDGE ---

    // 5. Bridge: Convert Prometheus metrics ke OTLP agar bisa dikirim lewat tunnel
    otelcol.receiver.prometheus "default" {
      output {
        metrics = [otelcol.processor.batch.default.input]
      }
    }

    // --- LOGS PIPELINE ---

    discovery.kubernetes "pods" {
      role = "pod"
    }

    // Filter: Hanya ambil log dari namespace development dan monitoring
    discovery.relabel "logs_filter" {
      targets = discovery.kubernetes.pods.targets
      rule {
        source_labels = ["__meta_kubernetes_namespace"]
        regex         = "development|monitoring"
        action        = "keep"
      }
    }

    loki.source.kubernetes "pods" {
      targets    = discovery.relabel.logs_filter.output
      forward_to = [loki.process.add_label.receiver]
    }

    loki.process "add_label" {
      forward_to = [otelcol.receiver.loki.default.receiver]
      stage.static_labels {
        values = {
          cluster = sys.env("CLUSTER_NAME"),
        }
      }
    }

    otelcol.receiver.loki "default" {
      output {
        logs = [otelcol.processor.batch.default.input]
      }
    }

    // --- PROCESSOR ---
    
    // Batch Processor mengumpulkan data sebelum dikirim (sangat penting untuk efisiensi tunnel)
    otelcol.processor.batch "default" {
      timeout = "5s"
      send_batch_size = 1000
      
      output {
        metrics = [otelcol.exporter.otlp.monitoring_server.input]
        logs    = [otelcol.exporter.otlp.monitoring_server.input]
      }
    }

    // --- EXPORTER ---
    
    otelcol.exporter.otlp "monitoring_server" {
      client {
        endpoint = sys.env("MONITORING_ENDPOINT")
        tls { insecure = true }
      }
      // Tambahkan queue agar lebih kuat menampung data
      sending_queue {
        enabled    = true
        queue_size = 10000
      }
    }
---
# Source: alloy/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: alloy
  labels:
    helm.sh/chart: alloy-1.8.1
    app.kubernetes.io/name: alloy
    app.kubernetes.io/instance: alloy
    app.kubernetes.io/version: "v1.16.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: alloy
    app.kubernetes.io/component: rbac
rules:
  - apiGroups:
    - ""
    - discovery.k8s.io
    - networking.k8s.io
    resources:
    - endpoints
    - endpointslices
    - ingresses
    - pods
    - services
    verbs:
    - get
    - list
    - watch
  - apiGroups:
    - ""
    resources:
    - pods
    - pods/log
    - namespaces
    verbs:
    - get
    - list
    - watch
  - apiGroups:
    - monitoring.grafana.com
    resources:
    - podlogs
    verbs:
    - get
    - list
    - watch
  - apiGroups:
    - monitoring.coreos.com
    resources:
    - prometheusrules
    verbs:
    - get
    - list
    - watch
  - apiGroups:
    - monitoring.coreos.com
    resources:
    - alertmanagerconfigs
    verbs:
    - get
    - list
    - watch
  - apiGroups:
    - monitoring.coreos.com
    resources:
    - podmonitors
    - servicemonitors
    - probes
    - scrapeconfigs
    verbs:
    - get
    - list
    - watch
  - apiGroups:
    - ""
    resources:
    - events
    verbs:
    - get
    - list
    - watch
  - apiGroups:
    - ""
    resources:
    - configmaps
    - secrets
    verbs:
    - get
    - list
    - watch
  - apiGroups:
    - apps
    - extensions
    resources:
    - replicasets
    verbs:
    - get
    - list
    - watch
  - apiGroups:
    - ""
    resources:
    - nodes
    verbs:
    - get
    - list
    - watch
  - apiGroups:
    - ""
    resources:
    - nodes/pods
    verbs:
    - get
    - list
    - watch
  - apiGroups:
    - ""
    resources:
    - nodes/metrics
    verbs:
    - get
    - list
    - watch
  - nonResourceURLs:
    - /metrics
    verbs:
    - get
---
# Source: alloy/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: alloy
  labels:
    helm.sh/chart: alloy-1.8.1
    app.kubernetes.io/name: alloy
    app.kubernetes.io/instance: alloy
    app.kubernetes.io/version: "v1.16.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: alloy
    app.kubernetes.io/component: rbac
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: alloy
subjects:
  - kind: ServiceAccount
    name: alloy
    namespace: monitoring
---
# Source: alloy/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: alloy
  namespace: monitoring
  labels:
    helm.sh/chart: alloy-1.8.1
    app.kubernetes.io/name: alloy
    app.kubernetes.io/instance: alloy
    app.kubernetes.io/version: "v1.16.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: alloy
    app.kubernetes.io/component: networking
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: alloy
    app.kubernetes.io/instance: alloy
  internalTrafficPolicy: Cluster
  ports:
    - name: http-metrics
      port: 12345
      targetPort: 12345
      protocol: "TCP"
---
# Source: alloy/templates/controllers/daemonset.yaml
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: alloy
  namespace: monitoring
  labels:
    helm.sh/chart: alloy-1.8.1
    app.kubernetes.io/name: alloy
    app.kubernetes.io/instance: alloy
    app.kubernetes.io/version: "v1.16.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: alloy
spec:
  minReadySeconds: 10
  selector:
    matchLabels:
      app.kubernetes.io/name: alloy
      app.kubernetes.io/instance: alloy
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: alloy
      labels:
        app.kubernetes.io/name: alloy
        app.kubernetes.io/instance: alloy
    spec:
      serviceAccountName: alloy
      containers:
        - name: alloy
          image: ghcr.io/maliqpotter/alloy:v1.16.1
          imagePullPolicy: IfNotPresent
          args:
            - run
            - /etc/alloy/config.alloy
            - --storage.path=/tmp/alloy
            - --server.http.listen-addr=0.0.0.0:12345
            - --server.http.ui-path-prefix=/
            - --stability.level=generally-available
            - --disable-reporting
          envFrom:
            - configMapRef:
                name: alloy-env
          env:
            - name: ALLOY_DEPLOY_MODE
              value: "helm"
            - name: HOSTNAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: K8S_NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          ports:
            - containerPort: 12345
              name: http-metrics
          readinessProbe:
            httpGet:
              path: /-/ready
              port: 12345
              scheme: HTTP
            initialDelaySeconds: 10
            timeoutSeconds: 1
          volumeMounts:
            - name: config
              mountPath: /etc/alloy
        - name: config-reloader
          image: ghcr.io/maliqpotter/prometheus-config-reloader:v0.81.0
          imagePullPolicy: IfNotPresent
          args:
            - --watched-dir=/etc/alloy
            - --reload-url=http://localhost:12345/-/reload
          volumeMounts:
            - name: config
              mountPath: /etc/alloy
          resources:
            requests:
              cpu: 10m
              memory: 50Mi
      dnsPolicy: ClusterFirst
      volumes:
        - name: config
          configMap:
            name: alloy
